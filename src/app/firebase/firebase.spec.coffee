describe('devcamp-angular-multeam devcamp-angular-multeam.firebase module', ()->
  beforeEach(module('devcamp-angular-multeam.firebase', () ->
    
  ))

  it('should have a dummy test', inject(() ->
    expect(true).toBeTruthy()
  ))
)